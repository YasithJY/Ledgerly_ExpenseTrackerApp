<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@color/card_bg"
    android:padding="20dp">

    <!-- Header Row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="16dp">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/ocr_title"
            android:textColor="@color/text_primary"
            android:textSize="17sp"
            android:textStyle="bold" />

        <ImageButton
            android:id="@+id/btnCloseOcr"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@android:drawable/ic_menu_close_clear_cancel"
            app:tint="@color/text_secondary"
            android:contentDescription="Close" />
    </LinearLayout>

    <!-- ========== STATE: IDLE ========== -->
    <LinearLayout
        android:id="@+id/stateIdle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center">

        <!-- Viewfinder box with CameraX PreviewView inside -->
        <FrameLayout
            android:id="@+id/layoutViewFinderContainer"
            android:layout_width="match_parent"
            android:layout_height="220dp"
            android:background="@drawable/bg_scan_viewfinder"
            android:layout_marginBottom="12dp"
            android:clickable="true"
            android:focusable="true">

            <androidx.camera.view.PreviewView
                android:id="@+id/viewFinder"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                app:scaleType="fillCenter" />

            <TextView
                android:id="@+id/tvCameraPlaceholder"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:text="Tap box to scan photo"
                android:gravity="center"
                android:textColor="@color/text_secondary"
                android:textSize="14sp" />
        </FrameLayout>

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Point your camera at a receipt, or upload an image to extract the total amount automatically using Google ML Kit OCR."
            android:textColor="@color/text_secondary"
            android:textSize="12sp"
            android:gravity="center"
            android:layout_marginBottom="16dp" />

        <!-- Upload Button -->
        <Button
            android:id="@+id/btnUploadImage"
            style="?attr/materialButtonOutlinedStyle"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:text="@string/ocr_upload"
            android:textColor="@color/primary_blue"
            app:strokeColor="@color/primary_blue"
            android:layout_marginBottom="20dp" />
    </LinearLayout>

    <!-- ========== STATE: SCANNING ========== -->
    <LinearLayout
        android:id="@+id/stateScanning"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:visibility="gone">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="180dp"
            android:background="@color/scanner_laser_bg"
            android:layout_marginBottom="16dp">

            <!-- Laser bar that animates up/down -->
            <View
                android:id="@+id/laserBar"
                android:layout_width="match_parent"
                android:layout_height="3dp"
                android:background="@color/primary_blue"
                android:layout_gravity="top" />
        </FrameLayout>

        <ProgressBar
            android:id="@+id/lottieScanning"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_marginBottom="12dp"
            android:indeterminateTint="@color/primary_blue" />

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/ocr_scanning"
            android:textColor="@color/text_secondary"
            android:textSize="12sp"
            android:gravity="center"
            android:layout_marginBottom="16dp" />
    </LinearLayout>

    <!-- ========== STATE: COMPLETE ========== -->
    <LinearLayout
        android:id="@+id/stateComplete"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:gravity="center"
        android:visibility="gone">

        <!-- Success badge -->
        <ImageView
            android:id="@+id/lottieSuccess"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:src="@android:drawable/ic_dialog_info"
            app:tint="@color/income_green"
            android:layout_marginBottom="8dp" />
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/ocr_success"
            android:textColor="@color/income_green"
            android:textSize="13sp"
            android:textStyle="bold"
            android:paddingHorizontal="16dp"
            android:paddingVertical="8dp"
            android:background="@drawable/bg_success_badge"
            android:layout_marginBottom="16dp" />

        <TextView
            android:id="@+id/tvDetectedAmount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Rs. 0.00"
            android:textColor="@color/text_primary"
            android:textSize="32sp"
            android:textStyle="bold"
            android:layout_marginBottom="6dp" />

        <TextView
            android:id="@+id/tvDetectedStore"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Store Name"
            android:textColor="@color/text_secondary"
            android:textSize="14sp"
            android:layout_marginBottom="4dp" />

        <TextView
            android:id="@+id/tvDetectedDate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Date"
            android:textColor="@color/text_secondary"
            android:textSize="12sp"
            android:layout_marginBottom="20dp" />

        <Button
            android:id="@+id/btnScanAgain"
            style="?attr/materialButtonOutlinedStyle"
            android:layout_width="match_parent"
            android:layout_height="44dp"
            android:text="@string/ocr_scan_again"
            android:textColor="@color/text_secondary"
            android:layout_marginBottom="8dp" />
    </LinearLayout>

    <!-- Footer Action Buttons (always visible) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="12dp">

        <Button
            android:id="@+id/btnOcrCancel"
            style="?attr/materialButtonOutlinedStyle"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:text="@string/ocr_cancel"
            android:textColor="@color/text_secondary"
            android:layout_marginEnd="8dp" />

        <Button
            android:id="@+id/btnFillAmount"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:text="@string/ocr_fill_amount"
            android:backgroundTint="@color/primary_blue"
            android:textColor="@color/white"
            android:enabled="false" />
    </LinearLayout>

</LinearLayout>
